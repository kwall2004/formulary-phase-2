/**
 * Created by agupta on 9/8/2016.
 */
/*
Ext.define('Atlas.authorization.view.AuthorizationController', {
    extend: 'Atlas.common.view.AppBaseController',
    alias: 'controller.authorizationController',
    btnSearchClick: function () {
        var view = this.getView(),
            searchWindow = view.add({
                xtype: 'authorization-cdagmainsearchwindow'
            });

        searchWindow.show();

    },

    GetPA: function (f, e) {
        if (e.getKey() == e.ENTER) {
            alert("about to submit from controller");
        }
    },

    onMemberSelection: function (combo, record) {
        //debugger;
        alert('Value selected');
    },

    onSelect_winCbxMember: function (combo, record) {

    }


});*/
